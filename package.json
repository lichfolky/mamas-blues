{
  "name": "11ty-blog",
  "version": "1.0.0",
  "description": "An [11ty](https://www.11ty.dev/) blog template",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && npx @11ty/eleventy",
    "dev": "rm -rf dist && npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "11ty",
    "template"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}